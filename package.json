{
  "name": "supernote-read-later",
  "version": "0.1.0",
  "description": "One-click read-later: URL -> clean EPUB (percollate container) -> Google Drive folder synced by a Supernote device",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "queue:create": "wrangler queues create read-later",
    "token": "node scripts/get-refresh-token.mjs"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
